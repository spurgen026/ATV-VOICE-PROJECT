openwakeword==0.6.0
faster-whisper==1.2.1
piper-tts==1.6.0
sounddevice==0.5.5
numpy
google-genai==1.47.0
faiss-cpu==1.13.0
pypdf==6.14.2
python-docx==1.2.0
python-dotenv==1.2.1

# V3: local Qwen router + CAN telemetry (see CLAUDE.md "Version 3 Plan")
llama-cpp-python==0.3.34
python-can==4.6.1

# V3 Phase 5: Tamil/Hindi TTS via MMS-TTS (Piper has no Tamil voices)
torch==2.13.0
transformers==5.14.1

# V3 hardening round 2: GPU acceleration for faster-whisper (RTX 3050 was
# unused - CPU STT latency was ~8-11s/query, GPU brings it to ~0.9s/query).
# Redistributable CUDA runtime DLLs, not a full CUDA Toolkit install - see
# stt.py for why plain `pip install` alone isn't enough (PATH setup needed).
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.24.0.43
nvidia-cuda-runtime-cu12==12.9.79

# V3 hardening round 2: real automated test suite in tests/, replacing the
# throwaway/uncommitted scripts this project relied on before.
pytest==9.1.1

# Professional polish: linting + type checking (config in pyproject.toml).
# mypy found and fixed several real gaps: two places where a Gemini API
# response field the SDK types as Optional was used without a None check
# (rag.py, index_documents.py - parked V2 code, but genuinely could crash
# if ever revived), and a variable-name collision in local_qa.py that
# would have raised NameError on a real, reachable code path.
ruff==0.16.2
mypy==2.3.0
